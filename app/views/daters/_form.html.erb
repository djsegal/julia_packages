<%= form_for(dater) do |f| %>
  <% if dater.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dater.errors.count, "error") %> prohibited this dater from being saved:</h2>

      <ul>
      <% dater.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :created %>
    <%= f.datetime_select :created %>
  </div>

  <div class="field">
    <%= f.label :updated %>
    <%= f.datetime_select :updated %>
  </div>

  <div class="field">
    <%= f.label :pushed %>
    <%= f.datetime_select :pushed %>
  </div>

  <div class="field">
    <%= f.label :package_id %>
    <%= f.text_field :package_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
