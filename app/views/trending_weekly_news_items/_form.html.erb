<%= form_for(trending_weekly_news_item) do |f| %>
  <% if trending_weekly_news_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trending_weekly_news_item.errors.count, "error") %> prohibited this trending_weekly_news_item from being saved:</h2>

      <ul>
      <% trending_weekly_news_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
